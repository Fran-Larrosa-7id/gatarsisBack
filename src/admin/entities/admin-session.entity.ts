import { Column, CreateDateColumn, Entity, Index, PrimaryGeneratedColumn } from 'typeorm';
@Entity({ name: 'admin_sessions' }) export class AdminSession { @PrimaryGeneratedColumn('uuid') id!: string; @Index() @Column({ name: 'admin_user_id', type: 'uuid' }) adminUserId!: string; @Column({ name: 'refresh_token_hash' }) refreshTokenHash!: string; @Column({ name: 'expires_at', type: 'timestamptz' }) expiresAt!: Date; @Column({ name: 'revoked_at', type: 'timestamptz', nullable: true }) revokedAt!: Date | null; @Column({ name: 'last_used_at', type: 'timestamptz', nullable: true }) lastUsedAt!: Date | null; @CreateDateColumn({ name: 'created_at', type: 'timestamptz' }) createdAt!: Date; }
